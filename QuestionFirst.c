#include <stdio.h> //Header file #include -> Presprocessor Directive  stdio.h ->Library

int main() // Main function
{
    printf("Hello Students"); // Printf is in build function which is defined in header file to print on string screen
    return 0;
}